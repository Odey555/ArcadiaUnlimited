ServerEvents.tags("item", arcadia => {
	
	
})


ServerEvents.recipes(arcadia => {
	
	// Removing recipes that are replaced by the more interesting versions
	//arcadia.remove({ id: '' })
	//arcadia.remove({ id: '' })

	
	
})